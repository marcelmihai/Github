# INFO: Get syslog-ng .conf names
- name: Get syslog-ng .conf names
  action: shell ls -lah /opt/splunk/Splunk-Configs/syslog-ng/buckets.d/| grep -rL ".conf"
  register: search_output
- debug: var=search_output.stdout_lines