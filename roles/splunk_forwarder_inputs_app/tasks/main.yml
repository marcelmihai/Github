- name: install forwarder inputs app
  action: shell cp -ax /export/home/mmuralikri/ansible4splunk/roles/splunk_forwarder_inputs_app/files/* "{{ forwarder_inputs_app_location }}"
  become: true
  become_user: "{{ SPLUNK_USER }}"
