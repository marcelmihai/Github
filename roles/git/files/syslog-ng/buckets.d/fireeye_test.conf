 #--------------------------------  test_fireeye ------------------------------------------------
 destination d_tfireeye { file ("/syslog/TFIREEYE/$SOURCEIP/$SOURCEIP.log"
                       owner(syslog-ng) group(splunk) perm(0755) dir_perm(0755) create_dirs(yes) );
                       };
                     	netmask ("10.16.68.46/32")           #HX
                        or netmask ("10.16.4.71/32")           #HX
 
 ;};
 log {source(s_network); filter(f_devicename);destination(d_devicename);};
